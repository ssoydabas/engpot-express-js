const remindTeachers = (students, reasonToSendMail) => {
    console.log(students)
    console.log(reasonToSendMail)

    switch(reasonToSendMail) {
        case "lessons":

            break
        case "speakingLessons":

            break
        case "assignments":

            break
    }

};

module.exports = remindTeachers;